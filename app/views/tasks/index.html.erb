<h1>Your Task List</h1>
<ul>
  <% @tasks.each do |task| %>
   <li>  [<%= task.completed == true ? 'X' : '  ' %>] <%= link_to task.title, task_path(task) %> </li>
  <% end %>
</ul>

<h3><%= link_to 'Add a new task', new_path %></h3>
